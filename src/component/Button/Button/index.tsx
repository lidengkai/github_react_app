import classnames from 'classnames'
import styles from './style.less'
import { ButtonNamespace } from '../interface'

const Button: FC<ButtonNamespace.Props> = memo(() => {
  return (
    <></>
  )
})

export default Button
